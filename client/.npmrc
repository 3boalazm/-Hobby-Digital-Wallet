# See ../.npmrc — same reasoning, kept here too in case npm install ever runs
# with this directory as its root (e.g. if Vercel's Root Directory project
# setting points here instead of the repo root).
legacy-peer-deps=true
